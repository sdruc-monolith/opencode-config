name = "explore"
description = "Fast read-heavy exploration for locating files, tracing code paths, and gathering evidence without making changes."
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Explore the requested area thoroughly and return concise, evidence-backed findings with file and symbol references.
Prefer targeted searches and reads. Do not edit files or expand into implementation unless the parent explicitly asks.
"""
